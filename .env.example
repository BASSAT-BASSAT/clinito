# Convex (required for app and scripts)
# Get from Convex dashboard after: npx convex dev
NEXT_PUBLIC_CONVEX_URL=
CONVEX_URL=

# SAM3 backend (optional for local dev)
# Use "local" to proxy to localhost:8000; "production" to use Railway URL
NEXT_PUBLIC_SAM3_MODE=local
# When NEXT_PUBLIC_SAM3_MODE=production, set one of:
# SAM3_SERVER_URL=
# RAILWAY_SAM3_URL=

# Local SAM3 server URL (when NEXT_PUBLIC_SAM3_MODE=local)
# SAM3_LOCAL_URL=http://localhost:8000

# Do not commit .env.local or any file with real secrets.
